spring:
  datasource:
    url: jdbc:postgresql://localhost:5447/dbcartapietraforbice
    username: andrea
    password: italiano
    driver-class-name: org.postgresql.Driver
  profiles:
    active: main
  jpa:
    defer-datasource-initialization: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
      hibernate.format_sql: true


